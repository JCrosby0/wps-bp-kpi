query boardMeeting($meeting: String) {
  meeting: meetings(where: { id: $meeting }) {
    id
    date
    year
    term
    week
    indicators {
      name
      boardDateLastNoted
      boardStatusAchieved
      boardStatusProgressNoted
    }
  }
}

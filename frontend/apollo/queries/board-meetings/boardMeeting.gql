query boardMeeting($meeting: String) {
  meeting: meetings(where: { id: $meeting }) {
    id
    date
    year
    term
    week
    presentations {
      Presenter
      Comments
      progressStatus
      progressAchieved
      indicator {
        name
      }
    }
  }
}
